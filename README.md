# QLD-farm-shapefile-extract
# The script uses geopandas to manipulate the shapefile data. 
# It aims to locate the specific farm location from QLD map database.
# Convert the type polygon into line or point.
# Export geojson  
